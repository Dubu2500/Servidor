# Servidor
Todas las tarea y trabajos de la materia servidor

## Chat en línea (socket.io)

- Ruta del chat: `GET /chat`


### Requisitos
- Node.js 18+ y npm

### Instalación
1. `npm install`
2. Copia `.env.example` a `.env` y ajusta si lo necesitas


Luego abre: `http://localhost:3000/chat`


